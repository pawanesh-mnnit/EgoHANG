Each model is trained and evaluated on a single participant.
Training uses 80% of the participant's annotated actions.
Validation uses the remaining 20%.
Split is done randomly with a fixed seed inside train.py (VAL_SPLIT = 0.2).
